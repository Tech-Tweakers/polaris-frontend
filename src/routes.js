import React from "react"; // Importe o React
import ChatUI from "chatui";

const routes = [
    {
        key: "chatui",
        route: "/",
        component: <ChatUI />,
    },
];

export default routes;
